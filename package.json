{
  "name": "nobyte",
  "version": "1.0.0",
  "description": "Free client-side tools that run in the browser. No uploads, no accounts.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "serve": "node serve.mjs",
    "start": "node build.mjs && node serve.mjs",
    "dev": "node build.mjs --watch & node serve.mjs",
    "test": "node --test test/*.test.js"
  },
  "license": "MIT"
}
